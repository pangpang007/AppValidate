import React from 'react';
import styles from './index.less';
import withMenu from '@/components/Menu';

const config = (props: object) => {
  return <div>111</div>;
};

export default withMenu(config)
